#pragma once
#include <cmath>

double gradient_descent1D(std::vector<double> X, std::vector<double> V, double eps)
{
	double X0 = X[X.size() / 2]; // Начальная точка
	double V_current = V[X0];
	double V_prev;
}

